model User {
  id             Int      @id @default(autoincrement())
  loginId        String   @unique
  password       String
  plainPassword  String?
  role           String   @default("USER")   // 👈 добавляем
  profile        Json?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}
